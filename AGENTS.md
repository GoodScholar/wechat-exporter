# 项目代理约定

## Agent skills

### Issue tracker

需求、规格和任务记录在 GoodScholar/wechat-exporter 的 GitHub Issues，使用 gh CLI 操作。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认的五个分流标签：needs-triage、needs-info、ready-for-agent、ready-for-human、wontfix。详见 `docs/agents/triage-labels.md`。

### Domain docs

采用 single-context：根目录 CONTEXT.md 与 docs/adr/。详见 `docs/agents/domain.md`。
